var jsdom = require('jsdom');

// Exposes globals in node.js and sets up the current process 
// to act like a browser environment.
// 
// @param {Function} callback

module.exports.setup = function(callback) {
}